## Summary
- add `.github/scripts/download_latest_artifact.py` to deterministically fetch the latest successful artifact by workflow file + artifact name on branch `main`
- update `.github/workflows/cbb_analytics.yml` to use Tier A artifact download and Tier B validated repo fallback for ESPN inputs
- add explicit BLOCKED integrity summary handling with event-based exit behavior (`workflow_dispatch` skip=0, scheduled/automated fail=1)
- always upload integrity reports plus analytics output allowlist
- add `scripts/analytics_smoke.py` and document behavior in `docs/WORKFLOWS_README.md`

## Problem fixed
Prevents hard failure when `INFRA-espn-data` artifact is temporarily unavailable while still refusing to run analytics on invalid/missing required inputs.

## Validation
- `py -3 .github/scripts/validate_workflows.py`
- `py -3 -c "import yaml, pathlib; yaml.safe_load(pathlib.Path('.github/workflows/cbb_analytics.yml').read_text(encoding='utf-8')); print('YAML_OK')"`
- `py -3 scripts/analytics_smoke.py`
