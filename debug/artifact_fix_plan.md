# Artifact Fix Plan (Agent D Planning Council)

## Orchestrator checkpoint
- Agent A: PASS
- Agent B: PASS
- Agent C: PASS
- Agent D decision: APPROVED FOR IMPLEMENTATION

## Changed file list
- `.github/scripts/download_latest_artifact.py`
- `.github/workflows/cbb_analytics.yml`
- `debug/artifact_payload_validation.json` (generated by helper during validation run)
- `debug/artifact_dependency_chain_audit.md` (validation/audit documentation)

## Functions to modify
- `ArtifactDownloadError` model in helper (add stage classification).
- `_api_json` for stage-aware failures.
- `_download_binary` to use JSON Accept and stage-aware diagnostics.
- `main` flow in helper to:
  - separate lookup vs metadata vs download vs extract failures,
  - write machine-readable payload diagnostics.
- `cbb_analytics.yml` shell wrappers around helper calls:
  - stop labeling every failure as "Artifact lookup failed",
  - print stage-aware guidance.

## New error taxonomy
- `LOOKUP_FAILED`
- `METADATA_FAILED`
- `DOWNLOAD_FAILED`
- `EXTRACT_FAILED`
- `VALIDATION_FAILED`

## New diagnostics
- Helper emits structured JSON:
  - artifact metadata (name/run/artifact id/size)
  - requested endpoint and Accept used
  - stage classification
  - HTTP status and response snippet on failure
- Workflow logs stage-aware error message and next steps.

## Validation checklist
1. Reproduce existing failure path before patch.
2. Run helper against known artifact (`INFRA-predictions-rolling`) after patch.
3. Confirm archive download succeeds and extraction works.
4. Confirm debug payload file is written and contains stage/status fields.
5. Confirm workflow wrapper strings no longer claim lookup failure when download fails.
6. Confirm no behavior change in artifact chain semantics.

## Regression scope across dependency chain
- Shared helper is currently used in `cbb_analytics.yml` for:
  - `INFRA-espn-data`
  - `INFRA-predictions-rolling`
  - `INFRA-ready-cbb-predictions-rolling`
- `market_lines.yml` and `cbb_predictions_rolling.yml` artifact publishing remains unchanged.
- `actions/download-artifact@v4` paths remain untouched.

## Expected post-fix behavior
- Lookup success + download success path logs:
  - `[INFO] Found artifact ...`
  - `[OK] Extracted artifact ...`
- On failures, helper emits correct stage and details.
- Wrapper logs:
  - `"Artifact download failed"` only when helper exits non-zero for non-lookup reasons.

## Rollback plan
1. Revert `.github/scripts/download_latest_artifact.py` and `.github/workflows/cbb_analytics.yml` to previous commit.
2. Re-run `cbb_analytics.yml` using existing artifact chain.
3. If needed, temporarily use direct `actions/download-artifact` on known run-id while reworking helper.

## Why this is minimal and robust for this project
- Single shared helper patch removes root cause centrally.
- Small workflow message tweak corrects misclassification without changing pipeline logic.
- Added diagnostics improve triage without changing artifact contracts.
