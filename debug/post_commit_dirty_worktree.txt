Observed root-cause pattern (from failing workflow run):
- Commit step stages only the target output list.
- Commit succeeds.
- Additional generated files remain modified/untracked in `data/` and `debug/`.
- Next command attempted `git pull --rebase` and failed:
  - `error: cannot pull with rebase: You have unstaged changes.`
  - `error: Please commit or stash them.`

Representative remaining dirty categories after commit:
- Generated `debug/*` audit/sample files
- Generated `health_summary.md`
- Other generated outputs not in the explicit commit manifest

Workflow fix:
- capture this status at runtime via:
  - `git status --porcelain | tee debug/post_commit_dirty_worktree.txt`
- then clean non-target generated files before rebase:
  - `git restore --worktree --staged .`
  - `git clean -fd`
